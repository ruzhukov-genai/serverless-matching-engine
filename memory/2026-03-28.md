# 2026-03-28 — RDS Proxy Codified in CloudFormation + Pool Tuning

## Summary
- ✅ RDS Proxy pool tuned via CLI (immediate effect)
- ✅ RDS Proxy added to CloudFormation (backend.yaml) — no longer manual
- ✅ SAM deployed successfully, new proxy verified end-to-end
- ✅ Old manual proxy (`sme-proxy-v2`) deleted
- ✅ Old manual IAM role (`sme-rds-proxy-role`) deleted

## RDS Proxy Pool Tuning

### Changes Applied
| Setting | Before (default) | After | Why |
|---|---|---|---|
| ConnectionBorrowTimeout | 120s | **15s** | Fail fast under saturation, let SQS retry |
| IdleClientTimeout | 1800s | **300s** | Match Lambda sqlx max_lifetime, reclaim idle conns faster |

### Unchanged (kept as-is)
- MaxConnectionsPercent: 100 (need all ~52 conns on t4g.small)
- MaxIdleConnectionsPercent: 50
- SessionPinningFilters: [] (no pinning)

## CloudFormation: RDS Proxy Now Managed

### New CF Resources (backend.yaml)
- `RDSSecret` — Secrets Manager secret for proxy auth
- `RDSProxyRole` — IAM role for RDS Proxy → Secrets Manager access
- `RDSProxy` — `AWS::RDS::DBProxy` with all tuned settings
- `RDSProxyTargetGroup` — Target group pointing to `RDSInstance`

### New CF Resources (network.yaml)
- `RDSEgressSelfPostgres` — Self-referencing SG egress on 5432 (required for proxy)
- `RDSEgressSecretsManager` — HTTPS egress for proxy → Secrets Manager

### Other Changes
- Removed `RDSProxyEndpoint` parameter from backend.yaml — now uses `!GetAtt RDSProxy.Endpoint`
- Added `RDSProxyEndpoint` to Outputs (backend + root template)
- Added `inline` to root template's `OrderDispatchMode` AllowedValues
- New proxy name: `serverless-matching-engine-proxy` (was `sme-proxy-v2`)
- New proxy endpoint: `serverless-matching-engine-proxy.proxy-cp3apgpybbhw.us-east-1.rds.amazonaws.com`

### Deleted Manual Resources
- Proxy: `sme-proxy-v2` (replaced by CF-managed `serverless-matching-engine-proxy`)
- IAM Role: `sme-rds-proxy-role` (replaced by CF-managed `RDSProxyRole`)
- Old Secrets Manager secret `sme/rds-credentials` still exists but no longer referenced

## Testing
- Cold start order: 575ms (ETH-USDT limit buy) — 201 OK
- Warm start orders: ~103ms — 201 OK
- Proxy target health: AVAILABLE
- API /api/pairs: 200 OK

## Files Changed
- `infra/stacks/backend.yaml` — RDS Proxy resources, removed parameter, updated DATABASE_URL ref
- `infra/stacks/network.yaml` — SG egress rules for proxy
- `infra/template.yaml` — Added `inline` to AllowedValues, added RDSProxyEndpoint output
