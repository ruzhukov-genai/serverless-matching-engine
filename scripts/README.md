# Scripts

Dev and operational scripts.

## Planned

| Script | Purpose |
|--------|---------|
| `local-dev-up.sh` | Start local environment (LocalStack + Redis + RabbitMQ via Docker Compose) |
| `load-test.sh` | Run order match load test and report throughput |
| `seed-orderbook.sh` | Seed Redis with test order book data |
| `tail-logs.sh` | Tail CloudWatch logs for all Lambdas |
